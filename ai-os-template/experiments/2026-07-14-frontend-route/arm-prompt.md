Implement the bounded frontend experiment in this directory.

Read `packet.json` and `index.html`. Follow the packet exactly. Modify only
`index.html`; do not create, rename, or delete any file. Do not commit, stage, push,
install dependencies, access the network, or inspect files outside this directory.
This is a one-pass arm: do not run `verify.py` or repair after seeing its output.
Stop after writing the implementation and give a concise final status.
